{{ if .Values.podDisruptionBudgets.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: coordinator-pdb
spec:
  minAvailable: {{ .Values.coordinator.replicaCount }}
  selector:
    matchLabels:
      app: coordinator
{{ end }}
