{{ if .Values.podDisruptionBudgets.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: kafka-pdb
spec:
  minAvailable: {{ .Values.kafka.replicaCount }}
  selector:
    matchLabels:
      app: kafka
{{ end }}
