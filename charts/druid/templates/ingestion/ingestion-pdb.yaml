{{ if .Values.podDisruptionBudgets.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: ingestion-server-pdb
spec:
  minAvailable: {{ .Values.ingestion.replicaCount }}
  selector:
    matchLabels:
      app: ingestion-server
{{ end }}
