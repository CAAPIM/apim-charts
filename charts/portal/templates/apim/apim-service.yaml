# Copyright (c) 2026 Broadcom Inc. and its subsidiaries. All Rights Reserved.
apiVersion: v1
kind: Service
metadata:
  name: apim
  labels:
    app: apim
    chart: {{ template "portal.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: apim-http
    - port: 8443
      targetPort: 8443
      protocol: TCP
      name: apim-https
    - port: 9443
      targetPort: 9443
      protocol: TCP
      name: apim-pm
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: apim-analytics
    - port: 9449
      targetPort: 9449
      protocol: TCP
      name: apim-datalake
    - port: 9446
      targetPort: 9446
      protocol: TCP
      name: tssg-enroll
    - port: 1885
      targetPort: 1885
      protocol: TCP
      name: apim-broker
    - port: 9448
      targetPort: 9448
      protocol: TCP
      name: apim-sso
    - port: {{ .Values.portal.kafka.proxy.listenPort | default 9191 }}
      targetPort: {{ .Values.portal.kafka.proxy.listenPort | default 9191 }}
      protocol: TCP
      name: kfk-proxy-boot
{{- $brokerStartPort := int (.Values.portal.kafka.proxy.brokerStartPort | default 9193) }}
{{- $replicaCount := int (.Values.kafka.kafka.replicaCount | default 1) }}
{{- $ordinalStart := 0 }}
{{- if and .Values.kafka.kafka.ordinals (hasKey .Values.kafka.kafka.ordinals "start") }}
{{- $ordinalStart = int .Values.kafka.kafka.ordinals.start }}
{{- end }}
{{- range $i := until $replicaCount }}
{{- $nodeId := add $ordinalStart $i }}
{{- $brokerPort := add $brokerStartPort $nodeId }}
    - port: {{ $brokerPort }}
      targetPort: {{ $brokerPort }}
      protocol: TCP
      name: kfk-proxy-brk-{{ $nodeId }}
{{- end }}
  selector:
    app: apim
  type: ClusterIP
  sessionAffinity: ClientIP