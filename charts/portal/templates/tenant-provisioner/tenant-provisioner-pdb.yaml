{{ if .Values.tenant-provisioner.pdb.create }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: tenant-provisioner-pdb
spec:
  minAvailable: {{ .Values.tenant-provisioner.pdb.minAvailable }}
  maxUnavailable: {{ .Values.tenant-provisioner.pdb.maxUnavailable }}
  selector:
    matchLabels:
      app: tenant-provisioner
{{ end }}
