{{ if and (not .Values.intelligence.useExistingPullSecret) (.Values.standalone) }}
{{ if or (.Values.intelligence.registryCredentials) (.Values.intelligence.imagePullSecret.enabled)  }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.global.pullSecret }}
  annotations:
    helm.sh/hook: pre-install
    helm.sh/hook-weight: "-10"
  labels:
    app: {{ template "intelligence.name" . }}
    chart: {{ template "intelligence.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: kubernetes.io/dockerconfigjson
data:
 {{- if .Values.intelligence.registryCredentials }}
 {{- range $key, $value := fromYaml .Values.intelligence.registryCredentials }}
 {{- if eq $key "data" }}
 {{- range $value }}
  .dockerconfigjson: {{ . }}
 {{- end }}
  {{- end }}
  {{- end }}
 {{- else }}
  .dockerconfigjson: {{ template "intelligence-imagePullSecret" . }}
 {{ end }}
{{ end }}
{{ end }}