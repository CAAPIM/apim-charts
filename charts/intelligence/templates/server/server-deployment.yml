apiVersion: apps/v1
kind: Deployment
metadata:
  name: intelligence-server
  labels:
    app: intelligence-server
    chart: {{ template "intelligence.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    {{- range $key, $val := .Values.global.additionalLabels }}
    {{ $key }}: "{{ $val }}"
    {{- end }}
    {{- range $key, $val := .Values.intelligence.additionalLabels }}
    {{ $key }}: "{{ $val }}"
    {{- end }}
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  replicas: {{ .Values.intelligence.replicaCount }}
  selector:
    matchLabels:
      app: intelligence-server
  template:
    metadata:
      labels:
        app: intelligence-server
        release: {{ .Release.Name }}
      {{- if  .Values.intelligence.podAnnotations }}
      annotations: {{- toYaml .Values.intelligence.podAnnotations | nindent 8 }}
      {{- if .Values.intelligence.forceRedeploy }}
        timestamp: {{ now | quote }}
      {{- end }}
      {{- end }}
      {{- if not .Values.intelligence.podAnnotations }}
      {{- if .Values.intelligence.forceRedeploy }}
      annotations:
        timestamp: {{ now | quote }}
      {{- end }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "intelligence.serviceAccountName" . }}
      automountServiceAccountToken: {{ .Values.intelligence.serviceAccount.automountServiceAccountToken }}
      {{- if .Values.intelligence.affinity }}
      affinity: {{- toYaml .Values.intelligence.affinity | nindent 12 }}
      {{- end }}
      {{- if .Values.intelligence.nodeSelector }}
      nodeSelector: {{- toYaml .Values.intelligence.nodeSelector | nindent 12 }}
      {{- end }}
      {{- if .Values.intelligence.tolerations }}
      tolerations: {{- toYaml .Values.intelligence.tolerations | nindent 12 }}
      {{- end }}
      {{- if .Values.intelligence.podSecurityContext }}
      securityContext: {{- toYaml .Values.intelligence.podSecurityContext | nindent 12 }}
      {{- else if .Values.global.podSecurityContext }}
      securityContext: {{- toYaml .Values.global.podSecurityContext | nindent 12 }}
      {{- end }}
      {{- if .Values.intelligence.kafka.autoDiscovery.enabled }}
      {{ template "intelligence.validate" .}}
      initContainers:
        - name: kafka-broker-discovery
          image: "alpine/k8s:1.31.2"
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -ec
            - |
              # This script discovers the external LoadBalancer IPs of Kafka brokers
              # and writes them to a shared volume for the main container to use.
              RELEASE_NAME="{{ .Release.Name }}"
              BROKER_LIST=""

              # Function to retry a command (sh-compatible)
              retry_while() {
                  cmd="${1:?cmd is missing}"
                  retries="${2:-12}"
                  sleep_time="${3:-5}"
                  return_value=1
                  i=1
                  while [ "$i" -le "$retries" ]; do
                      if eval "$cmd"; then
                          return_value=0
                          break
                      fi
                      echo "Retrying in ${sleep_time}s... (${i}/${retries})"
                      sleep "$sleep_time"
                      i=$((i + 1))
                  done
                  return $return_value
              }

              # Function to get LoadBalancer IP or Hostname from a service (sh-compatible)
              k8s_svc_lb_ip() {
                  service_ip=$(kubectl get svc "$1" -n "$2" -o jsonpath="{.status.loadBalancer.ingress[0].ip}" 2>/dev/null || echo "")
                  service_hostname=$(kubectl get svc "$1" -n "$2" -o jsonpath="{.status.loadBalancer.ingress[0].hostname}" 2>/dev/null || echo "")
                  if [ -n "$service_ip" ]; then 
                      echo "$service_ip"
                  elif [ -n "$service_hostname" ]; then 
                      echo "$service_hostname"
                  else 
                      return 1
                  fi
              }

              discover_broker() {
                  SVC_NAME=$1
                  BROKER_ENDPOINT=""

                  echo "Discovering broker: ${SVC_NAME}"

                  if ! kubectl get svc "$SVC_NAME" -n "$MY_POD_NAMESPACE" > /dev/null 2>&1; then
                    echo "Service $SVC_NAME not found, skipping."
                    return
                  fi

                  AUTODISCOVERY_SERVICE_TYPE=$(kubectl get svc "$SVC_NAME" -n "$MY_POD_NAMESPACE" -o jsonpath='{.spec.type}' 2>/dev/null || echo "")

                  if [ "$AUTODISCOVERY_SERVICE_TYPE" = "LoadBalancer" ]; then
                      if ! retry_while "kubectl get svc $SVC_NAME -n $MY_POD_NAMESPACE -o jsonpath='{.status.loadBalancer.ingress[0]}'" ; then 
                          echo "Timed out waiting for LoadBalancer IP for service $SVC_NAME"
                          exit 1
                      fi
                      EXTERNAL_ADDRESS=$(k8s_svc_lb_ip "$SVC_NAME" "$MY_POD_NAMESPACE")
                      EXTERNAL_PORT=$(kubectl get svc "$SVC_NAME" -n "$MY_POD_NAMESPACE" -o jsonpath="{.spec.ports[0].port}" 2>/dev/null || echo "")
                      BROKER_ENDPOINT="${EXTERNAL_ADDRESS}:${EXTERNAL_PORT}"
                  elif [ "$AUTODISCOVERY_SERVICE_TYPE" = "NodePort" ]; then
                      NODE_PORT=$(kubectl get svc "$SVC_NAME" -n "$MY_POD_NAMESPACE" -o jsonpath="{.spec.ports[0].nodePort}" 2>/dev/null || echo "")
                      NODE_IP=$(kubectl get nodes -o jsonpath='{.items[0].status.addresses[?(@.type=="ExternalIP")].address}' 2>/dev/null || kubectl get nodes -o jsonpath='{.items[0].status.addresses[?(@.type=="InternalIP")].address}' 2>/dev/null || echo "")
                      if [ -n "$NODE_IP" ] && [ -n "$NODE_PORT" ]; then
                        BROKER_ENDPOINT="${NODE_IP}:${NODE_PORT}"
                      fi
                  fi

                  if [ -n "$BROKER_ENDPOINT" ]; then
                    if [ -z "$BROKER_LIST" ]; then 
                        BROKER_LIST="$BROKER_ENDPOINT"
                    else 
                        BROKER_LIST="$BROKER_LIST,$BROKER_ENDPOINT"
                    fi
                  fi
              }

              # Discover Kafka broker nodes - Updated to use correct naming (kafka-0, kafka-1, kafka-2)
              # For custom Kafka subchart
              {{- if and .Values.kafka (hasKey .Values.kafka "kafka") .Values.kafka.kafka .Values.kafka.kafka.replicaCount }}
              BROKER_COUNT={{ .Values.kafka.kafka.replicaCount }}
              for i in $(seq 0 $((BROKER_COUNT - 1))); do
                SVC_NAME="kafka-${i}-external"
                discover_broker "$SVC_NAME"
              done
              {{- else if and .Values.kafka (hasKey .Values.kafka "controller") .Values.kafka.controller }}
              # For Bitnami Kafka chart (backward compatibility)
              {{- if and .Values.kafka.controller.replicaCount (not .Values.kafka.controller.controllerOnly) }}
              CONTROLLER_BROKER_COUNT={{ .Values.kafka.controller.replicaCount }}
              for i in $(seq 0 $((CONTROLLER_BROKER_COUNT - 1))); do
                SVC_NAME="kafka-controller-${i}-external"
                discover_broker "$SVC_NAME"
              done
              {{- end }}

              # Discover dedicated broker nodes
              {{- if and (hasKey .Values.kafka "broker") .Values.kafka.broker .Values.kafka.broker.replicaCount }}
              BROKER_COUNT={{ .Values.kafka.broker.replicaCount }}
              if [ "$BROKER_COUNT" -gt 0 ]; then
                for i in $(seq 0 $((BROKER_COUNT - 1))); do
                  SVC_NAME="kafka-broker-${i}-external"
                  discover_broker "$SVC_NAME"
                done
              fi
              {{- end }}
              {{- else }}
              # Fallback: No Kafka configuration found
              echo "Warning: No Kafka broker configuration found"
              {{- end }}

              echo "Discovered Kafka brokers: ${BROKER_LIST}"
              echo "export KAFKA_EXTERNAL_ADVERTIZED_BROKERS=${BROKER_LIST}" > /shared/kafka.env
          env:
            - name: MY_POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          volumeMounts:
            - name: shared-data
              mountPath: /shared
      {{- end }}
      containers:
        - name: intelligence-server
          image: "{{ .Values.global.portalRepository }}{{ .Values.intelligence.image.intelligenceServer }}"
          imagePullPolicy: "{{ .Values.intelligence.image.pullPolicy }}"
          {{- if .Values.intelligence.containerSecurityContext }}
          securityContext: {{- toYaml .Values.intelligence.containerSecurityContext | nindent 12 }}
          {{- else if .Values.global.containerSecurityContext }}
          securityContext: {{- toYaml .Values.global.containerSecurityContext | nindent 12 }}
          {{- end }}
          command: ["/bin/sh", "-c"]
          args:
            - |-
              {{- if .Values.intelligence.kafka.autoDiscovery.enabled }}
              # Source the environment file from the init container
              if [ -f /shared/kafka.env ]; then
                echo "Sourcing KAFKA_EXTERNAL_ADVERTISED_BROKERS from init container..."
                cat /shared/kafka.env
                source /shared/kafka.env
              else
                echo "WARNING: /shared/kafka.env not found. Auto-discovery may not have completed."
              fi
              {{- else }}
              echo "Using KAFKA_EXTERNAL_ADVERTISED_BROKERS from helm values..."
              export KAFKA_EXTERNAL_ADVERTIZED_BROKERS={{ .Values.intelligence.kafka.externalAdvertisedBrokers | quote }}
              {{- end }}
              
              # Load TSSG certificates for intelligence server if enabled
              {{- if .Values.intelligence.kafka.kafkaCa.caSecretName }}
              echo "Loading TSSG certificates for intelligence server..."
              # Assuming certificates are available as environment variables from secrets.
              # If they are mounted as files, the application needs to read them.
              # For now, relying on Kubernetes to inject as environment variables from secrets.
              {{- end }}
              
              echo "Executing original entrypoint for intelligence-server: java -jar /opt/app/intelligence-server.jar"
              exec java -jar /opt/app/intelligence-server.jar
          env:
            - name: RABBITMQ_DEFAULT_PASS
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.rabbitmq.auth.secretName }}
                  key: rabbitmq-password
                  optional: false
            - name: INTELLIGENCE_DATABASE_NAME
              value: {{ template "intelligence-db-name" . }}
            - name: DATABASE_USERNAME
              value: {{ .Values.global.databaseUsername | quote }}
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.databaseSecret }}
                  {{ if eq .Values.global.databaseType "mysql" }}
                  key: mysql-password
                  {{- end }}
          # TSSG Certificates (always use apim-ssl from internal secret)
          # Intelligence server expects certificate and key content in environment variables
          {{- if .Values.intelligence.kafka.kafkaCa.caSecretName }}
            - name: KAFKA_CA_CERTIFICATE
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.intelligence.kafka.kafkaCa.caSecretName }}
                  key: {{ .Values.intelligence.kafka.kafkaCa.caCertKey | default "apim-ssl.crt" }}
                  optional: false
            - name: KAFKA_CA_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.intelligence.kafka.kafkaCa.caSecretName }}
                  key: {{ .Values.intelligence.kafka.kafkaCa.caKeyKey | default "apim-ssl.p8.key" }}
                  optional: false
          {{- else }}
            # Provide empty values when using PLAINTEXT Kafka (no SSL)
            - name: KAFKA_CA_CERTIFICATE
              value: ""
            - name: KAFKA_CA_KEY
              value: ""
          {{- end }}
          {{- if .Values.intelligence.kafka.kafkaCa.passwordSecretName }}
            - name: KAFKA_CA_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.intelligence.kafka.kafkaCa.passwordSecretName }}
                  key: {{ .Values.intelligence.kafka.kafkaCa.passwordSecretKey | default "keypass.txt" }}
                  optional: false
            - name: TSSG_SSL_KEY_PASS
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.intelligence.kafka.kafkaCa.passwordSecretName }}
                  key: {{ .Values.intelligence.kafka.kafkaCa.passwordSecretKey | default "keypass.txt" }}
                  optional: false
          {{- else }}
            # Provide empty values when using PLAINTEXT Kafka (no password needed)
            - name: KAFKA_CA_PASSWORD
              value: ""
            - name: TSSG_SSL_KEY_PASS
              value: ""
          {{- end }}
            # Kafka External Advertised Brokers (for user-facing connections)
          {{- if not .Values.intelligence.kafka.autoDiscovery.enabled }}
          {{- if .Values.intelligence.kafka.externalAdvertisedBrokers }}
            - name: KAFKA_EXTERNAL_ADVERTIZED_BROKERS
              value: {{ .Values.intelligence.kafka.externalAdvertisedBrokers | quote }}
          {{- end }}
          {{- end }}
          {{- $hasPapiHost := false }}
          {{- $hasPapiPort := false }}
          {{- if .Values.intelligence.env }}
          {{- range $key, $value := .Values.intelligence.env }}
            {{- if eq $key "PAPI_PUBLIC_HOST" }}
              {{- $hasPapiHost = true }}
            {{- end }}
            {{- if eq $key "PAPI_PUBLIC_PORT" }}
              {{- $hasPapiPort = true }}
            {{- end }}
            - name: {{ $key }}
              value: {{ $value | quote }}
          {{- end }}
          {{- end }}
          {{- if not $hasPapiHost }}
            # Auto-generate PAPI_PUBLIC_HOST if not provided
            - name: PAPI_PUBLIC_HOST
              value: {{ include "intelligence.publicHost" . | quote }}
          {{- end }}
          {{- if not $hasPapiPort }}
            # Auto-generate PAPI_PUBLIC_PORT if not provided
            - name: PAPI_PUBLIC_PORT
              value: {{ include "intelligence.publicPort" . | quote }}
          {{- end }}
          envFrom:
          - configMapRef:
              name: intelligence-server-config
          readinessProbe:
            httpGet:
              path: "/health/readiness"
              port: 8282
            initialDelaySeconds: 90
            timeoutSeconds: 1
            periodSeconds: 15
            successThreshold: 1
          livenessProbe:
            httpGet:
              path: "/health/liveness"
              port: 8282
            initialDelaySeconds: 120
            timeoutSeconds: 1
            periodSeconds: 15
            successThreshold: 1
          {{- if .Values.intelligence.resources }}
          resources: {{- toYaml .Values.intelligence.resources | nindent 12 }}
          {{- end }}
          {{- if .Values.intelligence.kafka.autoDiscovery.enabled }}
          volumeMounts:
            - name: shared-data
              mountPath: /shared
          {{- end }}
          ports:
            - containerPort: 8282
      volumes:
      {{- if .Values.intelligence.kafka.autoDiscovery.enabled }}
        - name: shared-data
          emptyDir: {}
      {{- end }}
      {{- if .Values.global.pullSecret }}
      imagePullSecrets:
      - name: "{{ .Values.global.pullSecret }}"
      {{- end }}
      {{- if .Values.intelligence.affinity }}
      affinity: {{- toYaml .Values.intelligence.affinity | nindent 8 }}
      {{- end }}
      {{- if .Values.intelligence.nodeSelector }}
      nodeSelector: {{- toYaml .Values.intelligence.nodeSelector | nindent 8 }}
      {{- end }}
      {{- if .Values.intelligence.tolerations }}
      tolerations: {{- toYaml .Values.intelligence.tolerations | nindent 8 }}
      {{- end }}
      {{- if .Values.global.schedulerName }}
      schedulerName: "{{ .Values.global.schedulerName }}"
      {{- end }}
      restartPolicy: Always
      terminationGracePeriodSeconds: 30